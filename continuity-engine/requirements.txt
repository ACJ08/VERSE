pydantic>=2.6,<3
networkx>=3.2,<4
fastapi>=0.110,<1
uvicorn[standard]>=0.27,<1
python-multipart>=0.0.9,<1
python-jose[cryptography]>=3.3,<4
bcrypt>=4.0,<6
pypdf>=4.0,<6             # PDF text extraction in upload.py (replaces pymupdf)
python-dotenv>=1.0,<2     # Load .env into os.environ on startup

# optional – IBM watsonx (install separately when credentials are available)
# ibm-watsonx-ai>=1.0

# optional – LangChain LLM adapter (provider-agnostic; install the pieces you use).
# VERSE_LLM_MODEL="anthropic:claude-sonnet-5" selects the provider at runtime.
# langchain>=0.3
# langchain-anthropic>=0.3   # or langchain-ibm / langchain-openai / ...

# dev
pytest>=8.0
httpx>=0.27
